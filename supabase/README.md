# supabase — migrations, seed, functions, worker, crons
Migrations are the DDL in `docs/04-backend/schema.md`, one file per section (T03). Functions per
`docs/04-backend/api.md`; worker and crons per `jobs.md`. `dashboards.sql` holds the goal queries.
Use the `db-migrate` skill for any schema change.
