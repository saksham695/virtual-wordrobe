# packages/engine — pure TypeScript styling engine
Spec: `docs/04-backend/engine.md`. Rules: `knowledge/domain/styling-rules.md` (JSON block → `src/rules.ts`).
Tests: vitest, vectors from the spec. Build: tsup → ESM (Deno) + CJS (RN). First task: T10.
