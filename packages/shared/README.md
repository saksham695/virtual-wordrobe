# packages/shared — types, zod schemas, constants, taxonomy loader
Every API request/response (`docs/04-backend/api.md`), every AI JSON (`docs/06-ai/*.md`), the
`OutfitComposition` geometry (`src/composition.ts`, D12), quotas. First task: T01.
